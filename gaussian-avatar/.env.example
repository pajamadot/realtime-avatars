# =============================================================================
# Gaussian Avatar — Environment Variables
# Copy this file to .env and fill in your keys.
# =============================================================================

# --- LLM (pick one) ---

# Option A: OpenAI
OPENAI_API_KEY=sk-your-openai-key-here

# Option B: Alibaba DashScope (Qwen)
# DASHSCOPE_API_KEY=sk-your-dashscope-key-here

# --- TTS ---

# CosyVoice via DashScope (default in chat_with_lam.yaml)
# Uses the same DASHSCOPE_API_KEY above, or set separately:
# COSYVOICE_API_KEY=sk-your-key

# Alternative: EdgeTTS (free, no key needed — switch config to edge_tts)

# --- Avatar Face ---
# To switch faces, edit config/chat_with_lam.yaml > LamClient > asset_path
# Available: james.zip (default), barbara.zip, status.zip, vfhq_case1.zip
# Then restart: docker compose restart avatar

# --- Optional ---

# HuggingFace token (for gated model downloads)
# HF_TOKEN=hf_your-token-here
